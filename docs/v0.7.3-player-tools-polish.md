# v0.7.3 Player Tools Polish

## 基础版本

基于 v0.7.2 player tools 继续小改。

## 本次只改 UI

不改播放逻辑，不改播放栏，不改歌词，不改数据。

## 调整内容

1. 播放工具按钮整体缩小。
2. 按钮位置更靠近播放栏右侧。
3. 红色描边降低强度，避免过亮。
4. 默认状态更接近原网站灰黑玻璃风格。
5. 音量按钮不再长期红色强调。
6. 弹出菜单尺寸同步缩小。

## 主要替换文件

```txt
index.html
```
