# v0.7.5 Player Tools Position & Volume Polish

## 基础版本

基于 v0.7.4 minimal player tools 小改。

## 本次改动

1. 两个按钮整体向右移动，避免和播放器重叠。
2. 音量浮层直接对齐音量按钮正上方。
3. 音量浮层去掉所有文字，只保留一条滑轨。
4. 删除音量面板里的静音按钮结构。
5. 不改播放栏本体、不改歌词、不改数据、不改播放逻辑。

## 主要替换文件

```txt
index.html
```
