# v0.6.4 Lyrics UI Fix

## 修复内容

1. 未播放时不再用 `lyricPreview` 在左侧显示巨大歌词片段。
2. 隐藏旧的 `.left-caption`，避免和同步歌词面板冲突。
3. 左侧同步歌词区域去掉可见框/雾化底色，改成透明浮字。
4. 第一次点击播放时强制加载歌词并高亮当前行。
5. 播放事件触发时重新校准歌词状态。
6. 保留 `lyricsOffset` 单曲偏移机制。

## 目前规则

- 未播放：左侧显示“选择歌曲后显示同步歌词。”
- 播放中：左侧显示 LRC 同步歌词
- 无 LRC：显示无同步歌词提示
